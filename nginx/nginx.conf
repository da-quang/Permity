server_names_hash_bucket_size  64;

events {
    worker_connections 1024;
}
  
http {
 server {
   listen 80;
   listen [::]:80;

    server_name palmiest-hornet-1388.dataplicity.io.;
        
     location  / {
        # Reverse proxy for Next server
        proxy_pass http://172.16.238.13:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        add_header 'Content-Security-Policy' 'upgrade-insecure-requests';

        }
    }
    

 server {
   listen 80;
   listen [::]:80;

    server_name palmiest-hornet-1388.dataplicity.io.backend;

   
   
        location / {
        proxy_pass http://172.16.238.12:8090;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
   }
  }


 server {
   listen 80;
   listen [::]:80;

    server_name palmiest-hornet-1388.dataplicity.io.pgadmin;
              
    location  / {
        proxy_pass http://172.16.238.10:5050;
        #proxy_set_header X-Script-Name /pgadmin;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
    }        
 }
}
